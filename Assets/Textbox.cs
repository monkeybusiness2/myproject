using UnityEngine;

public class Textbox : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /*
           1. player presses f from the PlayerControls the text box is activated like the UI looks like a text box 
           2. the text is slightly animated. it reads like Yknow left to right slowly . space to show the whole text 

        */
        
    }
}
